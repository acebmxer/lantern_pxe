fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
sqlalchemy==2.0.52
passlib[bcrypt]==1.7.4
# Pinned to 4.0.x: passlib 1.7.4 can't read the version of bcrypt >=4.1 (noisy
# but harmless warning), and bcrypt >=5 is fatal — its hashpw() raises on
# secrets over 72 bytes, which passlib's load-time self-test triggers, so the
# app dies on startup.
bcrypt==4.0.1
python-multipart==0.0.32
itsdangerous==2.2.0
# TOTP second-factor authentication (RFC 6238).
pyotp==2.9.0
# QR rendering for the TOTP enrolment screen. Pure Python with no dependencies
# on 3.10+ (no Pillow, no C extension) and it emits SVG, so the code is inlined
# into the page — a Lantern with no internet access still shows a scannable QR.
segno==1.6.6
